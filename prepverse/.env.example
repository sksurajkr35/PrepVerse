# Backend base URL. Empty = same-origin /api
# (Vite proxy in dev, nginx proxy in the Docker setup).
VITE_API_URL=
# Split production deploy example:
# VITE_API_URL=https://prepverse-api.onrender.com
